# 三国游戏 · 项目结构说明

## 一、项目总览

本仓库是一个"三国"主题的游戏项目，同一套游戏代码维护 **三端**，内容保持一致：

| 目录 | 说明 |
| ---- | ---- |
| `Android/` | 安卓版（支持触屏，检测 `ANDROID_DATA` 环境变量自适应分辨率） |
| `san_guo/` | 三国桌面版（与 `Android/ASSET` 内容一致） |
| `PY C++/` | Python + C++ 整合版（包含语音识别、C++ 渲染等增强内容） |
| `ASSET/`（根目录） | 历史编译产物（.pyc），非源代码 |
| `VoicePlay/` | 语音播放相关辅助代码 |

三端的核心游戏代码均位于各自目录下的 `ASSET/` 文件夹中。
`Android/ASSET` 为主版本，可通过根目录的同步脚本分发到另外两端。

---

## 二、目录结构

```
san_guo/
├── Android/              # 安卓版（主版本）
│   ├── main.py           # 程序入口
│   └── ASSET/            # 核心游戏代码（全部功能模块）
├── san_guo/              # 桌面三国版（ASSET 与 Android 同步）
├── PY C++/               # PYandC++ 整合版（含 C++ 渲染、语音模块）
├── VoicePlay/            # 语音播放辅助代码
├── sync_to_py_cpp.py     # 三端同步工具（自动识别目录，可指定源端）
└── 项目结构说明.md        # 本文档
```

---

## 三、ASSET 核心模块清单（按功能分类）

### 1. 核心基础设施
| 模块 | 功能 |
| ---- | ---- |
| `game_data.py` | 全局数据 `data`、存档读写 `save()`、字体 `get_font()`、日志 `logger`、渐变背景等公共能力 |
| `game_main_menu.py` | 主菜单调度器：四个下拉菜单 + 模块路由表，负责分发所有功能 |
| `gameplay_systems.py` | 通用玩法系统支持 |
| `login_system.py` | 登录 / 注册 / 账号系统 |
| `modern_ui_system.py` | 现代 UI 组件库（下拉菜单、按钮等） |
| `font_manager.py` | 字体管理 |
| `languages.py` | 多语言文本支持 |
| `dictionary_system.py` | 词典/查询系统 |
| `effect_system.py` | 特效系统 |
| `fun_effects.py` | 趣味特效 |
| `event_system.py` | 事件系统 |
| `auto_update.py` | 自动更新 |
| `anti_decompile.py` | 反编译防护 |

### 2. 地图与战斗
| 模块 | 功能 |
| ---- | ---- |
| `game_map_pygame.py` | 2D 游戏地图 |
| `game_map_3d.py` | 3D 地图（默认生存模式，支持 `/gamemode` 指令） |
| `game_map_enhanced.py` | 增强版 2D 世界地图 |
| `strategy_map_system.py` | 战略地图系统 |
| `battle_system.py` | 副本挑战 / 战斗系统 |
| `dungeon_system.py` | 地牢副本 |
| `pvp_p2p.py` | PVP 点对点联机 |
| `pvp_online.py` | PVP 在线联机 |
| `pvp_super.py` | PVP 超级模式 |
| `network_pvp.py` / `secure_network.py` | 联机网络与加密 |
| `chat_server.py` / `world_chat.py` / `p2p_ngrok.py` | 聊天服务器 / 世界聊天 / 内网穿透 |

### 3. 武将与养成
| 模块 | 功能 |
| ---- | ---- |
| `hero_warehouse.py` | 武将仓库 |
| `hero_recruitment.py` | 武将招募 |
| `hero_collection_system.py` | 武将图鉴收集 |
| `talent_system.py` | 天赋系统 |
| `tech_tree.py` | 科技树系统 |
| `building_system.py` | 建筑系统 |
| `equipment_system.py` | 装备系统 |
| `fashion_system.py` | 时装系统 |

### 4. 宠物
| 模块 | 功能 |
| ---- | ---- |
| `pet_system.py` | 宠物系统（饲养、进化、超进化、究极进化） |
| `pet_arena.py` | 宠物竞技场 |

### 5. 玩法与活动
| 模块 | 功能 |
| ---- | ---- |
| `quest_system.py` | 任务系统（含每日任务 / 每周任务标签页） |
| `task_chain_system.py` | 每日任务链（被任务系统调用） |
| `daily_checkin.py` | 每日签到 |
| `daily_reward_system.py` | 每日奖励 |
| `achievement_system.py` | 成就系统 |
| `activity_system.py` | 活动中心 |
| `limited_time_events.py` | 限时活动 |
| `ranking_system.py` | 排行榜系统 |
| `fishing_system.py` | 钓鱼系统 |
| `alchemy_system.py` | 炼金系统 |
| `shop_system.py` | 游戏商城 |
| `trading_system.py` | 交易系统 |
| `social_system.py` | 社交系统 |
| `weather_system.py` | 天气系统 |
| `ai_system.py` | 游戏内 AI |
| `faq_system.py` | 疑难解答 |
| `newbie_guide.py` | 新手引导 |
| `background_story.py` | 背景故事 |

### 6. 休闲小游戏
| 模块 | 功能 |
| ---- | ---- |
| `snake_game.py` | 贪吃蛇 |
| `push_box.py` | 推箱子 |
| `breakout.py` | 打砖块 |
| `minesweeper.py` | 扫雷 |
| `game_2048.py` | 2048 |
| `tetris.py` | 俄罗斯方块 |
| `gobang.py` | 五子棋 |
| `racing_mode.py` | **竞速模式（新增）**：三国主题无尽跑酷 |

### 7. 其它
| 模块 | 功能 |
| ---- | ---- |
| `whiteboard.py` | 白板涂鸦 |

---

## 四、主菜单路由机制

`game_main_menu.py` 是全部功能的调度中心，采用 **模块路由表** 设计：

```python
MODULE_ROUTES = {
    "1":  "pvp_p2p.py",            # 菜单编号 -> 模块文件
    "33": "racing_mode.py",        # 竞速模式（新增）
    ...
}
SPECIAL_HANDLERS = {
    "10": mini_games_menu,         # 需要特殊逻辑的菜单项
}
```

- 菜单点击事件不再使用冗长的 `if/elif` 链，统一查表分发（`run_module()` 动态导入并调用模块 `main()`）。
- **新增功能三步走**：① 写模块文件（含 `main()`）→ ② 在 `MODULE_ROUTES` 登记一行 → ③ 在菜单项列表中加入对应条目。
- 本次已补齐菜单中原本"可见但无响应"的 6 个入口：
  - `33` 竞速模式 → `racing_mode.py`（新玩法）
  - `34` 生存模式 → `game_map_3d.py`（3D 地图默认生存模式）
  - `35` 宠物进化 → `pet_system.py`
  - `36` 每日任务 → `quest_system.py`
  - `37` 限时活动 → `limited_time_events.py`
  - `38` 宠物竞技场 → `pet_arena.py`

---

## 五、三端同步方法

修改代码后，运行根目录的同步工具即可分发：

```bash
python sync_to_py_cpp.py                  # 默认以 Android/ASSET 为源同步到另两端
python sync_to_py_cpp.py --source san_guo # 指定源端
python sync_to_py_cpp.py --skip-asset     # 仅同步语音文件
```

- 该工具自动识别脚本所在目录（无需修改路径）。
- 同步时仅覆盖目标端**已存在的同名文件**，不会删除各端独有内容（如 `PY C++` 的 C++ 集成文件）。

---

## 六、运行方式

- 桌面版：在 `san_guo/` 或 `Android/` 目录运行 `python main.py`
- 安卓版：通过 `p4a` / 打包脚本构建 APK（目录内含 `.bat` 打包脚本）
- 三端共用同一套存档数据结构（`game_data.data`）
